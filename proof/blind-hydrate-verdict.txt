All 7 acceptance scenarios pass and the harness exits 0.

I read `quiz.seed.md`, generated `quiz.html` from its contracts (a single self-contained file — embedded 5-question bank, vanilla-JS quiz engine with per-question remembered selections, clamped Next/Prev navigation, Submit scoring with unanswered-as-wrong, Restart reset; inline CSS/JS, no external resources), then wrote a Playwright harness (`verify.mjs`) implementing the seed's primitives and all 7 acceptance scenarios driving the real rendered page via `file://`.

Two fixes during verification — both correct without weakening the contract:
- The `#restart` button was nested inside the hidden `#result` panel, so it was unclickable while the quiz was active. Made it always visible (and stopped hiding the quiz body on submit).
- A bug in my own harness: `goto()` was restarting on every step inside `answerAll()`, wiping prior answers. Fixed it to navigate from the current index via Next/Prev without restarting.

Final verdict:
```
[1] initial render ✓
[2] navigation + clamp ✓
[3] selection persists ✓
[4] perfect → score 5/5 ✓
[5] mixed → score 3/5 ✓
[6] unanswered counts wrong → score 1 ✓
[7] restart resets ✓
[grep] no external resource references ✓
VERIFY: 7/7 scenarios passed   (exit 0)
```

SEED_RESULT=DONE
